<html>
<head><title><%= @room.name %></title></head>
<body>
<h1><%= @room.name %></h1>
<div id="messages">
  <%= render partial: "messages/message", collection: @messages, as: :message %>
</div>
<%= form_with url: room_messages_path(@room), method: :post do |f| %>
  <%= f.text_field :body, name: "message[body]" %>
  <%= f.submit "Send" %>
<% end %>
</body>
</html>
