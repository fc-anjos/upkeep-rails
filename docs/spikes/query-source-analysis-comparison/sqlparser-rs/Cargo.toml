[package]
name = "upkeep-sqlparser-spike"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlparser = { version = "0.62.0", features = ["visitor"] }
